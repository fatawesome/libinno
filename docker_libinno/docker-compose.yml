version: '3'
services:
  web:
    build: .
    ports:
      - '8000:8000'
    volumes:
      - ..:/inno_lib
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: '${POSTGRES_PASSWORD}'
    volumes:
      - ../postgres_data:/var/lib/postgresql/data
